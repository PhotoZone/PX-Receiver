{
  "name": "px-receiver",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/desktop run dev",
    "clean:next-conflicts": "node scripts/clean-next-conflicts.mjs",
    "build:web": "npm run clean:next-conflicts && npm --workspace apps/desktop run build",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "set:version": "node scripts/set-version.mjs",
    "typecheck:web": "npm run clean:next-conflicts && npm --workspace apps/desktop exec -- tsc --noEmit",
    "test:web-contract": "node --test --experimental-strip-types apps/desktop/lib/receiver-contract.test.mts",
    "test:worker-contract": "PYTHONPATH=worker python3 -m unittest discover -s worker/tests -p 'test_*.py'",
    "lint:web": "npm --workspace apps/desktop run lint",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
