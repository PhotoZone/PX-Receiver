{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for the receiver UI in dev and packaged builds.",
  "windows": ["main"],
  "local": true,
  "remote": {
    "urls": [
      "http://localhost:3000",
      "http://127.0.0.1:3000"
    ]
  },
  "permissions": [
    "core:default"
  ]
}
